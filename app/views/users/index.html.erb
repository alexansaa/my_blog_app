<head>
  <%= stylesheet_link_tag "user_index" %>
</head>
<body>
  <div class="space_btw">
    <% @users.each do |user| %>
      <%= render partial: 'user', locals: { user: user } %>
    <% end %>
  </div>
  <div>
    <%= link_to 'New Post', user_post_new_path %>
  </div>
</body>
