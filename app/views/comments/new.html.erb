<h1>New Comment</h1>
<%= form_with model: [@user, @post, @comment], url: user_post_comments_path(@user, @post), method: :post, local: false, scope: :comment do |f| %>
  <div>
    <%= f.label :text, 'Comment Text' %>
    <%= f.text_area :text %>
  </div>
  <%= f.submit 'Create Comment' %>
<% end %>